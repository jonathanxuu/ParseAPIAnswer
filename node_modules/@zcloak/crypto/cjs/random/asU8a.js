"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.randomAsHex = randomAsHex;
exports.randomAsU8a = randomAsU8a;
var _util = require("@polkadot/util");
var _cross = require("@zcloak/cross");
// Copyright 2021-2023 zcloak authors & contributors
// SPDX-License-Identifier: Apache-2.0

/**
 * @name randomAsU8a
 * @summary Creates a Uint8Array filled with random bytes.
 * @description
 * Returns a `Uint8Array` with the specified (optional) length filled with random bytes.
 * @example
 * <BR>
 *
 * ```javascript
 * import { randomAsU8a } from '@zcloak/crypto';
 *
 * randomAsU8a(); // => Uint8Array([...])
 * ```
 */
function randomAsU8a() {
  let length = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 32;
  return (0, _cross.getRandomValues)(new Uint8Array(length));
}

/**
 * @name randomAsHex
 * @description Creates a hex string filled with random bytes.
 */
function randomAsHex() {
  let length = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 32;
  return (0, _util.u8aToHex)((0, _cross.getRandomValues)(new Uint8Array(length)));
}